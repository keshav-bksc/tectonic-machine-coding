# Tectonic machine coding

## Steps to start the app

Clone the repository and then in the project directory, run the below:

### `npm install`
### `npm start`


# Summary 

Used React.js and CSS along with react-router to build the app.
